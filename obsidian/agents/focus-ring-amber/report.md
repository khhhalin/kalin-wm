---
status: for-review
---

# focus-ring-amber — report

## Change

`code/config/config.def.h:13` — `focuscolor` changed from `COLOR(0x005577ff)`
(inherited dwl teal-blue) to `COLOR(0xf0a030ff)` (warm amber, per
`obsidian/implementation/focus-ring.md` "Color (decided 2026-07-18)" spec).
`bordercolor` and `urgentcolor` untouched.

`code/config/config.h` was **not** edited directly: it doesn't exist as a
tracked file in this worktree (it's gitignored — `.gitignore:6` — and
generated by `make` from `config.def.h` if absent, per `Makefile:117`). The
`make clean all` verification run below regenerated it from the updated
`config.def.h`, confirmed at `code/config/config.h:13` post-build:
`static const float focuscolor[] = COLOR(0xf0a030ff);`. Nothing to commit for
it since it's not tracked.

## Verification

- `nix develop -c make clean all` — exit 0, full clean build succeeded.
- `nix develop -c make test-unit` — 25/25 bind-parser tests passed, 0
  assertion failures; window-shader-math tests all passed. No test moved
  (as expected for a pure color constant change).

## Out of scope, not attempted

The bar-over-ring layering fix (`dwl.c` scene-layer change, noted in
`focus-ring.md` under "Layering vs docked chrome") — explicitly excluded from
this task's scope, owned by a separate sequence.

## Notes

`obsidian/implementation/focus-ring.md` was read but not edited, per brief —
its Color section already documents this change as canon.
