# IPC socket

- The IPC socket is a unix-domain socket that exposes the compositor's state to external programs.
- Its path is exported in the `$KALIN_IPC_SOCKET` environment variable, set before [[kalin-wm]] forks its startup command (so children inherit it).
- It is implemented in the `ipc.c` runtime module.

- The socket broadcasts [[viewport]]/compositor state as newline-delimited JSON (camera x/y/zoom, [[follow-mode]] flags, [[crop-mode]] active, focused window).
- It accepts camera commands from clients: `pan`, `zoom`, `zoom-reset`, `follow-toggle`.
- **Agent input synthesis (2026-08-13)**: `click`, `key`, `type` drive the live seat with synthetic pointer/keyboard input via `wlr_seat_*` — no external virtual-input protocol (the deferred input half of [[agent-observation]], now built). Each replies one JSON line like `screenshot-window` does.
  - `click [x y] [btn]` — synthetic pointer click. With `x y` (layout pixels, same coords as `warp`) it warps there first (reusing warp's `wlr_cursor_warp`+`motionnotify` path) so pointer-enter lands on that surface, then presses; without them it clicks at the current cursor. `btn` is `left`/`right`/`middle` or a raw evdev code, default `BTN_LEFT`. Emits press→frame→release→frame. Reply `{"type":"click","ok":true}`.
  - `key <keysym>` — one keypress on the **currently keyboard-focused** window (does NOT auto-focus; caller should `focus <id>` first). `<keysym>` is an xkb keysym *name* (`Return`, `Escape`, `a`) or `0x…` hex. Reply `{"type":"key","ok":true|false,...}` (false for an unknown keysym or no keyboard/unmappable).
  - `type <utf8>` — types a string into the keyboard-focused window one codepoint at a time; the argument is the rest of the line verbatim (spaces preserved, not tokenized). `\n`→Return, `\t`→Tab. Reply `{"type":"type","ok":true,"typed":N,"skipped":M}` (unmappable codepoints are logged and skipped, never mis-typed).
  - **The reverse-map** (`ipc_keysym_to_evdev()` in `ipc.c`): xkb only maps keycode+level→keysym forward, so it scans every keycode/level (layout 0) for one that produces the target keysym. Level→modifier: L0 none, **L1 Shift**, **L2 AltGr** (`ISO_Level3_Shift`/Mod5); levels ≥3 are reported and skipped. The modifier is applied as a *mask* via `wlr_seat_keyboard_notify_modifiers()` bracketing the key — a raw modifier *keycode* alone does not change the glyph the client derives (a client tracks the effective mask from the seat `modifiers` event, not by re-running xkb over key presses; seen live: bracketing with a Shift keycode typed `>` as `.`).
  - **Security capability**: any socket-holder gets full synthetic pointer+keyboard input into any window (roughly a keylogger's write side). Acceptable because kalin-wm is a personal single-user compositor and the socket already lives in `$XDG_RUNTIME_DIR` (user-only, 0700); same trust boundary `warp`/spawn already assume. Framed in a code comment above the helpers, like `warp`'s "test/automation hook" note.
  - **Headless caveat**: on the headless backend the seat advertises **no keyboard capability** (kalin-wm only sets `WL_SEAT_CAPABILITY_KEYBOARD` when a physical keyboard device exists — inherited dwl logic, `dwl.c` ~1757), so no client binds a keyboard and `key`/`type` reach nothing. Verify on the **wayland** backend (nested inside a real session), which inherits a keyboard capability. `screenshot-window` likewise only composites under the wayland backend. Integration-verified there (typed `echo kalintest > file` into a focused `foot`, confirmed both the on-screen text and the resulting file); no unit test since the reverse-map needs a live xkb keymap.
- It also accepts window-docking commands: `dockprep <appid> <x> <y> <w> <h>` arms a one-shot "dock this app_id straight into this rect the moment it maps" request (`dockprep_register()`/`dockprep_consume()` in `dwl.c`) — send *before* spawning a panel's backing terminal, since `dock` itself would no-op on an app_id that doesn't exist as a client yet. Makes a panel's first-ever spawn dock straight from its first frame: no flash at a default floating position, no camera jump chasing it there. `dock <appid> <x> <y> <w> <h>` pins a client (found by app_id) into an exact borderless screen rect, exempt from camera pan/zoom; `undock <appid>` releases it. See `setdocked()` in `dwl.c` — lets a shell panel embed a real, fully interactive client (not a rendered texture) at a fixed spot in its own QML layout. The first time a client is ever docked, `setdocked()` permanently tags it `c->ispanel` — unlike `c->docked` (transient — false again during the brief undock-before-minimize window on close), this never clears, and is the actual compositor-wide "this is shell-panel chrome, not a real window" check: excluded from the taskbar (never gets a foreign-toplevel handle), camera-follow, `Super+0`/`Super+O` fit-all, `Super+Tab` cycling, and `Super`+arrow directional focus.
- `minimize <appid> <0|1>` hides/shows a client by app_id (wraps `setminimized()`) — pairs with dock/undock so a docked panel can fully disappear on close and reappear already-running on reopen.
- The state broadcast also includes `"dock_hover":"<appid>"|null` — the app_id of whichever docked client the cursor currently sits over, or `null`. Updated in `motionnotify()` (`dwl.c`) alongside the existing `xytonode()` hit-test, only broadcasting on an actual enter/leave transition. Exists because a docked client is a real Wayland toplevel, not QML content — the shell has no `HoverHandler`-equivalent way to know the cursor is over it on its own; the compositor is the only thing that knows. Consumed by `KalinViewport.dockHoverAppId` in the shell to auto-close a docked panel when the cursor leaves it (see [[quickshell-shell]]'s `DockedPanel.qml` section).
- **Output/display settings**: the state broadcast includes `"outputs":[{"name","x","y","width","height","refresh","scale","enabled","modes":[{"width","height","refresh"},...]},...]` — every connected monitor's current state and full advertised mode list, built in `ipc_build_state()` from the `mons` list directly (no `wlr-randr` subprocess needed). `set-output <name> <w> <h> <refresh> <scale> <x> <y> <enabled>` reconfigures one by output name via `ipc_set_output()` (`dwl.c`) — the same underlying `wlr_output_state`/commit path `outputmgrapplyortest()` uses for an external wlr-output-management-v1 client (that path, inherited from dwl, already fully implements apply/test — confirmed working by hand, not something this project had to build), just addressed by name instead of a client-supplied config object. `<w>`/`<h>` `<=0` leaves the mode unchanged, `<scale><=0` leaves scale unchanged, so a caller repositioning or disabling an output doesn't need to already know its current mode/scale.
- **Screenshots** (2026-07-15): `screenshot-ui` opens the interactive [[screenshot-ui]] (same as Super+Shift+S), `screenshot` fires an immediate whole-monitor capture (same as Super+Print, lands in `$KALIN_SHOT_DIR` or `$HOME`) — for shell widgets/scripts, and for driving a nested test instance where key injection isn't possible.
- **Single-window capture** (2026-08-12): `screenshot-window <id:N|app-id:X> [WxH] [path]` captures ONE window to a PNG via `capture_window()` (`capture.c`), which composites just that client's scene subtree offscreen (`shaders_capture_window()`, `shaders.c`) — so it's **occlusion-independent**: it captures the whole window even if it's behind others or partly off-screen, not just the visible part. Size is the window's on-screen size (native at camera zoom 1; a shot taken while zoomed comes out scaled, since kalin-wm zooms content via buffer `dst_size` — verified in the VM: foot captured 696×494 at zoom 1, ~411×292 zoomed out 5 notches). Optional `WxH` nearest-neighbour-scales the shot only, never the live window (verified: `400x300` → exactly 400×300). **This is the one command that replies** (the socket is otherwise broadcast-only): a single JSON line `{"type":"screenshot-window","ok",path,width,height}` sent on the requesting client's fd, so `ipc_exec_command()` now takes the `ipc_client *`. Built for agent-driven visual verification (grab one window instead of the whole screen), tested via the vmctl harness + a VM `kalin-ipc` helper. `id` is canonical (unique, from the `clients` feed); app-id hits the first match.
- **Brightness**: the state broadcast includes `"brightness":{"value":<raw>,"max":<raw>}|null` (raw sysfs-style values, not percent; `null` if no backlight device exists). `set-brightness <value>` sets it. Implemented in `code/src/modules/backlight.c` via **systemd-logind's `SetBrightness` D-Bus method**, not a direct sysfs write — the backlight brightness file is root-owned with no group-write udev rule on the reference host, so a plain-user sysfs write fails; logind already runs as root and exposes exactly this as an authenticated per-session action. Requires the calling process to genuinely belong to an active foreground logind session (falls back from `GetSessionByPID` to scanning `ListSessions` for a real seated session if the caller itself isn't tracked by one, but `SetBrightness` itself still checks the *caller's* session/credentials — no session-path argument can substitute for that).

- The state broadcast includes `"overview":true|false` — mirrors [[overview-mode]]'s `overview_is_active()`. Was originally added so the shell could show the connection-graph lines whenever overview is open, not just while Super is held (the two conditions used to be conflated; see the [[ledger]]). The `connections`/`pending_connect` state fields and the `sever` command were **removed 2026-08-13** with the [[connection-graph]] (layout Phase 1 — [[layout-impl]]); the shell's `ConnectionLines.qml`/`LineGeometry.qml` were deleted the same day (kalin-shell `5788f49`), so nothing reads them any more.

- **Taskbar feed (2026-07-16, for the [[app-launcher|TUI-bar]] direction)**: the
  state broadcast includes `"clients":[{"id","appid","title","focused","minimized"},...]`
  — every mapped, non-`ispanel` toplevel, by stable `Client.id`. Exists because a
  terminal-hosted bar (the planned kitty/Textual bar) cannot speak
  [[foreign-toplevel]]; QML shells should keep using the protocol. Same
  truncation discipline the other broadcast arrays use (partial entry erased on
  overflow; `IPC_BUF_SIZE` bumped to 24576 for it). Paired command: `focus <id>` —
  unminimizes, focuses, and `viewport_center_on()`s the client (a taskbar click
  on an infinite canvas must also bring the window into view).
- The state broadcast also carries `focused.yellow` (papyrus knob, see [[shaders]]).

- The [[quickshell-shell]] uses the socket (via its `KalinViewport` service) to mirror the camera and drive it.
- The socket complements [[foreign-toplevel]], which carries the window list; together they are the shell-integration channel.
