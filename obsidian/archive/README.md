# archive (frozen)

Frozen material — **not current truth**, kept for history. Do not treat any note
here as describing the compositor as it is now; the living vault is
`implementation/` (as-built) + `plan/` (intent).

- `research/` — deep one-time design/background research (scaling algorithms, niri
  comparators, protocol matrices). Superseded by shipped code in `implementation/`.
- `agents/` — fleet-run agent briefs/reports (historical).
- `tasks/` — fleet task specs (historical).

Moved here in the 2026-08-10 lean-prune. Chronology is in git.
