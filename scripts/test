#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"
if [[ "${1:-}" == "coverage" ]]; then
  exec nix develop -c make test-unit-coverage
fi
exec nix develop -c make test-unit
