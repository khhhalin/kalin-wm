# Code

Primary compositor code now lives here:

- `code/config/` — configuration (`config.h`, `config.def.h`, `config.mk`)
- `code/include/` — headers and protocol-generated headers
- `code/src/` — compositor sources and runtime modules
